const postActivity = require('./postActivity');

const getActivities = require('./getActivities');
const getActivity = require('./deleteActivity');

const putActivity = require('./putActivity');

const deleteActivity = require('./deleteActivity');

module.exports = {
    postActivity,
    getActivities,
    getActivity,
    putActivity,
    deleteActivity
}